<%# Partial that is rendered into the tab on the profile dashboard %>

<ul>
  <% @themes.each do |theme| %>
    <li> <%= link_to theme.name, theme_study_sessions_path(theme), data: { turbo_method: :post } %></li>
     <div class="template-card-custom" style="background: url(<%= url_for(theme.background) %>)"></div>
  <% end %>
</ul>
