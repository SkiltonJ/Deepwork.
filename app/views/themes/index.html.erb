<h1>hello</h1>


















<%= link_to "Quickstart", theme_study_sessions_path(@themes.first), data: { turbo_method: :post } %>

<ul>
  <% @themes.each do |theme| %>
    <li><%= link_to theme.name, theme_study_sessions_path(theme), data: { turbo_method: :post } %></li>
  <% end %>
</ul>
